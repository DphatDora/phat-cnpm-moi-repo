# This Project is a Frontend UI for Fullstack Express + React system

Base function: Register user, login, view User's profile. Using JWT for secure and authorization.

Init project: `npm i`
Run server: `npm run dev`
